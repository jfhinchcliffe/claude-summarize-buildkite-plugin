#!/bin/bash
set -euo pipefail

# Get the API key directly from Buildkite secrets and export it
APIKEY=$(buildkite-agent secret get ANTHROPIC_API_KEY)
export BUILDKITE_PLUGIN_CLAUDE_CODE_API_KEY="$APIKEY"

echo "API key set successfully"